{% extends "UNAHSGOBundle::layout.html.twig" %}
{% block content %}
<section class="row">
  <aside class="span3">
    <nav class="sidebar-nav">
      <ul class="nav nav-stacked nav-tabs">
        <li>
          <a href="{{ path('tiposolicitud') }}">Regresar a Tipos de Solicitudes</a>
        </li>
      </ul>
    </nav>
  </aside>
  <section class="span6">
    <form action="{{ path('tiposolicitud_create') }}" method="post" {{ form_enctype(form) }}>
      <fieldset>
        <legend>
          Agregar una Tipo de Solicitud
        </legend>
        {{ form_widget(form) }}
        <div class="form-actions">
          <button class="btn btn-primary" type="submit">
            Crear
          </button>
        </div>
      </fieldset>
    </form>
  </section>
</section>
{% endblock %}
