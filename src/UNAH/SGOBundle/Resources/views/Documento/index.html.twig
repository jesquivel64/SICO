{% extends "UNAHSGOBundle::layout.html.twig" %}
{% block content %}
<div class="row">
  <section class="span12">
    <hgroup class="page-header">
      <h1>Sistema Integrado de Correspondencia <small>Estad&iacute;sticas</small></h1>
    </hgroup>
  </section>
</div>
<div class="row">
  <section class="span6">
    <form action="{{ path('documento_estadistica_periodo') }}" method="get" {{ form_enctype(periodo_form) }}>
      <fieldset>
        <legend>
          Estad&iacute;sticas Por Fecha
        </legend>
        <input type="hidden" name="_method" value="GET" />
        {{ form_widget(periodo_form) }}
      </fieldset>
      <div class="form-actions">
        <button class="btn btn-primary" type="submit">
          Buscar
        </button>
      </div>
    </form>
  </section>
  <section class="span6">
    <form action="{{ path('documento_estadistica_resumen') }}" method="get" {{ form_enctype(resumen_form) }}>
      <fieldset>
        <legend>
          Resumen De Documentos Recibidos
        </legend>
        <input type="hidden" name="_method" value="GET" />
        {{ form_widget(resumen_form) }}
      </fieldset>
      <div class="form-actions">
        <button class="btn btn-primary" type="submit">
          Buscar
        </button>
      </div>
    </form>
  </section>
</div>
<div class="row">
  <section class="span6">
    <form action="{{ path('documento_estadistica_periodo_tipo') }}" method="get" {{ form_enctype(periodo_tipo_form) }}>
      <fieldset>
        <legend>
          Estad&iacute;sticas Por Tipo de Documento y Fecha
        </legend>
        <input type="hidden" name="_method" value="GET" />
        {{ form_widget(periodo_tipo_form) }}
      </fieldset>
      <div class="form-actions">
        <button class="btn btn-primary" type="submit">
          Buscar
        </button>
      </div>
    </form>
  </section>
  <section class="span6">
    <form action="{{ path('comentario_estadisticas') }}" method="get" {{ form_enctype(comentario_form) }}>
      <fieldset>
        <legend>
          Estad&iacute;sticas de Seguimiento
        </legend>
        <input type="hidden" name="_method" value="GET" />
        {{ form_widget(comentario_form) }}
      </fieldset>
      <div class="form-actions">
        <button class="btn btn-primary" type="submit">
          Buscar
        </button>
      </div>
    </form>
  </section>
</div>
{% endblock %}
