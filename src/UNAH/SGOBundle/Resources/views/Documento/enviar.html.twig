{% extends "UNAHSGOBundle::layout.html.twig" %}

{% block content %}
<section class="row-fluid">
  <aside class="span3">
    <nav class="sidebar-nav no-print">
      <ul class="nav nav-tabs nav-stacked no-print">
        <li>
          <a href="{{ path('documento_documento', { 'tipo': tipo.id }) }}">Regresar a  {{ tipo.nombre }}s</a>
        </li>
        <li>
          <a href="{{ path('documento_search') }}">Buscar Documentos</a>
        </li>
      </ul>
    </nav>
  </aside>
  <section class="span3">
    <hgroup class="page-header">
      <h1>Enviar un {{ tipo.nombre}}</h1>
    </hgroup>
  </section>
</section>
<section class="row-fluid">
  <section class="span9 offset3">
    <form action="{{ path('documento_enviar', {'tipo' : tipo.id}) }}" method="post" {{ form_enctype(form) }}>
      {{ form_widget(form) }}
      <p>
        <button type="submit">
          Create
        </button>
      </p>
    </form>
</section>
</section>
{% endblock %}
