{% extends "UNAHSGOBundle::layout.html.twig" %}
{% block content %}
<section class="row-fluid">
  <aside class="span3">
    <nav class="well sidebar-nav">
      <ul class="nav nav-list">
        <li>
          <a href="{{ path('tipodocumento') }}">Regresar a Tipos de Documentos</a>
        </li>
      </ul>
    </nav>
  </aside>
  <section class="span6">
    <form action="{{ path('tipodocumento_create') }}" method="post" {{ form_enctype(form) }}>
      <fieldset>
        <legend>
          Agregar una Tipo de Documento
        </legend>
        {{ form_widget(form) }}
        <div class="form-actions">
          <button class="btn btn-primary" type="submit">
            Crear
          </button>
        </div>
      </fieldset>
    </form>
  </section>
</section>
{% endblock %}
